/**
 * /coach Layout - Coach-specific routing
 */

export default function CoachLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="coach-layout">
      {children}
    </div>
  );
}
